<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DOCTYPE -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="web.dao.face.MemberDao">
	
	<select id="getMember_codeDao"
		parameterType="String"
		resultType="int">
		
		select member_code from member
		where member_id = #{loginid }
	</select>
	
	<update id="updateInfoDao"
		parameterType="web.dto.Member">
		
		update member 
		set
		member_name = #{member_name }, member_gender = #{member_gender }, member_birth = #{member_birth }, member_email = #{member_email }, 
				member_phone = #{member_phone }, member_address = #{member_address }	
	
	</update>
	
	<insert id="insertImg"
		parameterType="web.dto.UserImg">
			insert into UserImg( fileno, member_code, originname, storedname)
			values( UserImg_seq.nextval, #{member_code }, #{originname }, #{storedname })
	</insert>
	
	<select id="selectImgDao"
		parameterType="int"
		resultType="web.dto.UserImg">
	
		select * from userimg
		where member_code = #{member_code }
	</select>
	
	<select id="checkImg"
		parameterType="int"
		resultType="int">
		
		select count(*) from userimg
		where member_code = #{member_code }
	</select>
	
	<delete id="deleteImg"
		parameterType="int">
		delete from userimg where member_code = #{member_code }
	</delete>

	<insert id="insertLogin" parameterType="web.dto.Member">
		INSERT INTO member (member_code, member_id, member_pw, member_name, member_gender, member_birth, member_email, member_phone, member_address) 
		VALUES (member_seq.nextval, #{member_id }, #{member_pw }, #{member_name }, #{member_gender }, #{member_birth }, #{member_email }, #{member_phone }, #{member_address })
	</insert> 
	
	<select id="selectCntLogin" parameterType="web.dto.Member" resultType="int">
		SELECT COUNT(*) FROM member WHERE id = #{id } AND pw = #{pw }
	</select> 
	
	<select id="selectByMember" parameterType="web.dto.Member" resultType="web.dto.Member">
		SELECT * FROM member
		WHERE id = #{id }
	</select>
  
	<select id="joinCheckid" parameterType="web.dto.Member" resultType="int">
		select count(*) from member where member_id = #{member_id }
	</select> 

</mapper>